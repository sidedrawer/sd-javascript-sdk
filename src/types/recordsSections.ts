

export interface RecordsSections {

    sectionDescription?: boolean,
    sectionFiles?: boolean,
    sectionLinkedRecords?: boolean,
    sectionCollaborators?: boolean,
    sectionSpecificFields?: boolean,
    sectionCustomFields?: boolean,

}
