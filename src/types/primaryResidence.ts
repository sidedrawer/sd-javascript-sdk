export interface PrimaryResidence {
    buildingNumber?: string,
    streetName?: string,
    unitNumber?: string,
    city?: string,
    provinceState?: string,
    postalZip?: string,
    country?: string,

}