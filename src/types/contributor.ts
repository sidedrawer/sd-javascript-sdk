import { DisplayValue } from './displayValue'

export interface Contributor {

    openId?: string,
    firstName?: string,
    lastName?: string,
    email?: string,
    phoneNumber?: string,
    teamId?: string,

}
